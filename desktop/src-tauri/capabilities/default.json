{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal grants for the Morpheus operations webview: window, webview, and event operations only. No shell, filesystem, HTTP, or process capability is granted (DESK-001).",
  "windows": ["main"],
  "permissions": ["core:default"]
}