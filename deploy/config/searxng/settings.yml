use_default_settings: true

general:
  instance_name: Morpheus Search

server:
  bind_address: 0.0.0.0
  port: 8080
  limiter: false
  image_proxy: false

search:
  safe_search: 1
  autocomplete: ""
  formats:
    - html
    - json

ui:
  static_use_hash: true
